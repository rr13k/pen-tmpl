package api

import (
	"{{.Name}}/internal/app/api/handler"
	"{{.Name}}/internal/app/toolkit/router"
)

func RegisterApi() {

	router.UrlGroup("/machine",
		router.Url("/apple", handler.MachineApple),
		router.Url("/release", handler.ReleaseMachine),
	)

	router.Init()
}
